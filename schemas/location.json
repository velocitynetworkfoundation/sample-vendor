{
  "title": "location",
  "type": "object",
  "$id": "location",
  "properties": {
    "countryCode": {
      "type": "string",
      "format": "regex",
      "pattern": "^[A-Z]{2}$"
    },
    "regionCode": {
      "type": "string",
      "format": "regex",
      "pattern": "^[A-Za-z0-9]{1,3}$"
    }
  },
  "additionalProperties": false
}